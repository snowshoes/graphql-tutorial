mutation SignupMutation($user: SignupUserInput!) {
  singupUser(input: $user) {
    id
    username
    addresses {
      type
      streetName
      streetNumber
      floor
      city
      country
      postCode
    }
    emails
    phones {
      countryCode
      number
      type
    }
    profile {
      title
      firstName
      lastName
      avatar
      gender
    }
  }
}

mutation {
  createCat(input: { name: "kitty", age: 2 }) {
    id
    name
    age
  }
}

mutation {
  singupUser(input: { username: "James", password: "Bond" }) {
    id
    username
    password
  }
}

mutation {
  register(
    input: { username: "Jackie", email: "jackie@gmail.com", password: "123" }
  ) {
    id
    username
    email
  }
}
